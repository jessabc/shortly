type StatsData = {
    img: string,
    title:string,
    description: string
}

// typing manipulated data retrieved from api (InputForm.tsx), to save to linkData hook at Home.tsx
type Link = {
    originalLink: string,  
    shortLink: string,
}

